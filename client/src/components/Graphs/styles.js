import { makeStyles } from "@mui/styles"

// Create styles
export default makeStyles(() => ({
	graphContainer: {
		padding: 10,
		display: 'flex',
		flexDirection: 'row',
		alignItems: 'center'
	}
}))
